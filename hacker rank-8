SELECT name || '(' || SUBSTR(occupation,1,1) || ')'
FROM OCCUPATIONS

UNION ALL

SELECT 'There are a total of ' || COUNT(*) || ' ' 
       || LOWER(occupation) || 's.'
FROM OCCUPATIONS
GROUP BY occupation
ORDER BY 1;
